name: build
on:
  workflow_dispatch:
  push:
    branches: [ "v2" ]

env:
  ACTIONS_ALLOW_UNSECURE_COMMANDS: true
  compile-flags: "-c Release"
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '8.0.x'
    - name: Build OverrideLauncher.Core
      run: dotnet build src/OverrideLauncher.Core/OverrideLauncher.Core.csproj ${{ env.compile-flags }}
    - name: Set Version
      run: |
          $version = "$(Get-Date -Format 'yy.M.d.HHmm')"; $file = 'src/OverrideLauncher.Core/OverrideLauncher.Core.csproj'; (Get-Content $file) -replace '<Version>.*?</Version>', "<Version>$version</Version>" | Set-Content $file
    - name: Publish OverrideLauncher.Core
      uses: brandedoutcast/publish-nuget@v2.5.2
      with: 
        PROJECT_FILE_PATH: src/OverrideLauncher.Core/OverrideLauncher.Core.csproj
        NUGET_KEY: ${{ secrets.NUGET_API_KEY }}